{% extends 'base.html.twig' %}

{% block body %}
    <h1>{{ operation }} d'un produit</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="{{ operationBtn }}" class="btn btn-success">
    {{ form_end(form) }}

{% endblock %}

